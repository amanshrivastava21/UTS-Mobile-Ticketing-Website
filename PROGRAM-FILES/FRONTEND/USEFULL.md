# 🧩 Tailwind CSS Cheatsheet

A quick reference for commonly used Tailwind CSS utility classes, grouped by category with small use cases — perfect for development and rapid prototyping.

---

## 🧱 Spacing (Margin & Padding)

| Class     | Use Case |
|-----------|----------|
| `p-5`     | Padding all sides: `padding: 1.25rem;` |
| `pt-2`    | Padding top |
| `pb-4`    | Padding bottom |
| `px-3`    | Padding left & right |
| `py-1`    | Padding top & bottom |
| `m-4`     | Margin all sides |
| `mt-2`    | Margin top |
| `mb-6`    | Margin bottom |
| `mx-auto` | Horizontally center element |
| `-mt-2`   | Negative top margin |

---

## 🎨 Colors

| Class         | Use Case |
|---------------|----------|
| `bg-blue-500` | Background color |
| `text-red-600`| Text color |
| `border-gray-300` | Border color |
| `hover:bg-green-400` | Background on hover |
| `placeholder-gray-500` | Input placeholder color |

---

## 🔡 Text & Font

| Class            | Use Case |
|------------------|----------|
| `text-xl`        | Extra large text |
| `text-sm`        | Small text |
| `font-bold`      | Bold text |
| `font-light`     | Light font weight |
| `italic`         | Italic text |
| `underline`      | Underline text |
| `tracking-wide`  | Letter spacing |
| `truncate`       | Ellipsis overflow text |
| `text-center`    | Center align text |

---

## 🧍 Flexbox & Grid

| Class             | Use Case |
|-------------------|----------|
| `flex`            | Use flexbox |
| `inline-flex`     | Inline flex container |
| `flex-col`        | Column layout |
| `items-center`    | Vertically center items |
| `justify-between` | Space between items |
| `gap-4`           | Gap between children |
| `grid`            | Use CSS Grid |
| `grid-cols-3`     | 3-column grid |
| `col-span-2`      | Span 2 columns |

---

## 🪟 Layout / Box Model

| Class           | Use Case |
|------------------|----------|
| `container`      | Center content, max-width |
| `w-full`         | Full width |
| `w-1/2`          | 50% width |
| `h-screen`       | Full viewport height |
| `max-w-md`       | Max width medium |
| `min-h-full`     | Min height 100% |
| `box-border`     | Border-box model |

---

## 🌐 Positioning

| Class        | Use Case |
|--------------|----------|
| `relative`   | Relative positioning |
| `absolute`   | Absolute positioning |
| `fixed`      | Fixed positioning |
| `top-0`      | Top offset 0 |
| `left-4`     | Left offset 1rem |
| `z-10`       | Z-index: 10 |

---

## 🖼️ Backgrounds

| Class              | Use Case |
|--------------------|----------|
| `bg-cover`         | Cover entire element |
| `bg-center`        | Centered background |
| `bg-no-repeat`     | Prevent repetition |
| `bg-gradient-to-r` | Gradient left to right |
| `from-blue-500`    | Start color |
| `to-green-400`     | End color |

---

## 🧱 Borders & Radius

| Class         | Use Case |
|---------------|----------|
| `border`      | 1px border |
| `border-2`    | 2px border |
| `border-red-500` | Red border |
| `rounded`     | Rounded corners |
| `rounded-full`| Full circle (pill/avatar) |
| `rounded-lg`  | Large radius |

---

## ✨ Shadows & Effects

| Class         | Use Case |
|---------------|----------|
| `shadow`      | Default shadow |
| `shadow-md`   | Medium shadow |
| `shadow-lg`   | Large shadow |
| `shadow-inner`| Inner shadow |
| `opacity-50`  | 50% opacity |
| `transition`  | Enable transitions |
| `duration-300`| 300ms duration |
| `ease-in-out` | Smooth transition |

---

## 🖱️ Hover / Focus / Active

| Class              | Use Case |
|--------------------|----------|
| `hover:bg-blue-600`| Change bg on hover |
| `focus:outline-none`| No focus ring |
| `active:scale-95`  | Shrink on click |
| `hover:shadow-lg`  | Add shadow on hover |

---

## ⏱️ Animations

| Class          | Use Case |
|----------------|----------|
| `animate-spin` | Spinner/loader |
| `animate-bounce`| Bounce effect |
| `animate-ping` | Pulse animation |

---

## 🌐 Responsiveness

| Class            | Use Case |
|------------------|----------|
| `sm:text-sm`     | Small screen font size |
| `md:w-1/2`       | 50% width on medium screens |
| `lg:flex-row`    | Flex direction row on large |
| `xl:grid-cols-4` | 4 columns on XL screens |

---

## 🔒 Visibility & Display

| Class         | Use Case |
|---------------|----------|
| `hidden`      | Hide element |
| `block`       | Block display |
| `inline-block`| Inline-block |
| `flex`        | Flex container |
| `visible`     | Make visible |
| `invisible`   | Hide, but keep space |

---

## 🧪 Miscellaneous Utilities

| Class           | Use Case |
|------------------|----------|
| `cursor-pointer` | Make clickable |
| `select-none`    | Disable text selection |
| `overflow-auto`  | Scroll when overflow |
| `overflow-hidden`| Hide overflow |
| `resize`         | Enable resizing |
| `list-none`      | No list style |
| `divide-x-2`     | Horizontal divider |

---

> ✨ **Tip:** Combine these classes freely for rapid UI building. For full documentation, visit [Tailwind CSS Docs](https://tailwindcss.com/docs).

